# Production Configuration
# This file is loaded when spring.profiles.active=prod

# Server Configuration
server.port=${SERVER_PORT:8080}
server.servlet.context-path=/api/v1

# MongoDB Configuration
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://dev_divisha_app:Divisha%40123@cluster0.btsspaw.mongodb.net/crm_prod?appName=Cluster0}
spring.data.mongodb.auto-index-creation=true

# Connection Pool Settings
spring.data.mongodb.min-pool-size=10
spring.data.mongodb.max-pool-size=50

# JWT Configuration
jwt.secret=${JWT_SECRET:p8wtD7ADE1hGQBnoD9zxc+YkWYXiWrjmbwhKnatRXZaUoq77/0/dQhmWVd+6bl1Vj4VVKKv2Cim/EKKOnMskXw==}
jwt.expiration=86400000

# Logging - Less verbose in production
logging.level.root=INFO
logging.level.com.ultron.backend=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://crm-frontend.onrender.com}

# Jackson Configuration
spring.jackson.default-property-inclusion=non_null

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.mongo.enabled=false
spring.autoconfigure.exclude=org.springframework.boot.actuate.autoconfigure.mongo.MongoHealthContributorAutoConfiguration

# Disable Swagger in Production
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# Security Headers
server.error.include-message=never
server.error.include-stacktrace=never
