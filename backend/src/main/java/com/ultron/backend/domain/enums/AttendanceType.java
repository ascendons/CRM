package com.ultron.backend.domain.enums;

public enum AttendanceType {
    OFFICE,        // Working from office
    REMOTE,        // Work from home
    FIELD,         // Field work (sales visits, client meetings)
    HYBRID,        // Combination (started home, moved to office)
    CLIENT_SITE    // Working at client location
}
