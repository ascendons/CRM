package com.ultron.backend.domain.enums;

public enum ShiftType {
    FIXED,         // Fixed timings
    FLEXIBLE,      // Flexible timings within a window
    ROTATIONAL     // Rotating shifts (day/night)
}
